# Leave DYNAMODB_TABLE empty for memory-only development.
DYNAMODB_TABLE=headwins-poker-data-game
DYNAMODB_HISTORY_TABLE=headwins-poker-data-history
AWS_REGION=us-east-1
# Local development only; deployed backends should use their workload IAM role.
AWS_PROFILE=lawang
